specVersion: "v1beta"
serviceId: "health_app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "health_app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
